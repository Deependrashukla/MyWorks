# class Fraction(object):
#  # """Instances are fractions n/d"""
#  # numerator: int
#  # denominator: int > 0
#     def __eq__(self,q):
#     # """Returns: True if self, q equal, 
#      # False if not, or q not a Fraction"""
#         if (not (hasattr(q,'numerator') and hasattr(q,'denomenator'))): 
#             return False
#         left = self.numerator*q.denominator
#         rght = self.denominator*q.numerator
#         return left == rght
# x = Fraction(3,2)
# y = Fraction(3,2)

print(float('inf'))